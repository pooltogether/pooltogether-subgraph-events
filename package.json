{
  "name": "pooltogether-v2x-events",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/pooltogether-v2x-events",
    "create-local": "graph create --node http://localhost:8020/ pooltogether/pooltogether-v2x-events",
    "remove-local": "graph remove --node http://localhost:8020/ pooltogether/pooltogether-v2x-events",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pooltogether/pooltogether-v2x-events"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.14.0",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
